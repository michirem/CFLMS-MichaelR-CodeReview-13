<form method="post" action="{{ path('events_delete', {'id': event.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event.id) }}">
    <button class="btn btn-primary m-2">Delete</button>
</form>
